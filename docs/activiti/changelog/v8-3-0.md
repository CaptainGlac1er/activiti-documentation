---
slug: /changelog/8.3.0
title: "8.3.0"
sidebar_label: "8.3.0"
description: "Activiti 8.3.0 release notes: Nashorn script engine integration, decimal primitive type, Spring Boot 3.2."
---

# Version 8.3.0

**Released:** 2024-03-05

## New Features

- **Nashorn script engine integrated** — JavaScript (Nashorn) scripting support is now part of the core, so script tasks and script-based constructs can run without extra configuration (AAE-14392).
- **Decimal primitive type** — a new primitive type registered for decimal process variables (AAE-19479).

## Bug Fixes

- Fixed an issue where execution listeners were executed when a process instance is deleted (MNT-24081).
- Fixed subprocess with end-terminate-event and user task with boundary events (MNT-24153).
- Fixed MSSQL create and upgrade schema scripts.
- Identity link table column changed from `varbinary(max)` to `LONGBLOB`.

## Notable Changes

- Upgraded to Spring Boot 3.2.x (3.2.1, then 3.2.2).

## Breaking Changes & Mitigations

- **JavaScript script tasks now work out of the box** — the Nashorn engine is bundled in the core, so `scriptFormat="javascript"` no longer requires an external script engine dependency. Applications that shaded or explicitly provided their own JavaScript engine may now have two engines registered. **Mitigation:** remove your own script engine dependency and rely on the bundled one.
- **New `bigdecimal` variable type** — a new primitive type is registered in the process extension variable type map. Additive, but note that numeric values declared with `bigdecimal` are parsed via `BigDecimalVariableType`. **Mitigation:** none required.
- **Schema fixes** — the identity link details column moved from `varbinary(max)` to `LONGBLOB` on MySQL and the MSSQL create/upgrade scripts were corrected. **Mitigation:** none required; the engine applies the upgrade scripts.

## Use Cases

### JavaScript script tasks without extra dependencies

With the Nashorn engine bundled in the core, script tasks can use JavaScript out of the box — no additional script engine dependency in your application:

```xml
<scriptTask id="greet" name="Greet" scriptFormat="javascript">
  <script><![CDATA[
    var name = execution.getVariable("customerName");
    execution.setVariable("greeting", "Hello " + name);
  ]]></script>
</scriptTask>
```

### Precise numeric variables

Process variables can now be declared with the `bigdecimal` type in the process extension JSON. Values are parsed to `BigDecimal` (from numbers or numeric strings) and validated as numeric, avoiding floating-point rounding issues for monetary or measurement data:

```json
{
  "properties": {
    "amount": {
      "name": "amount",
      "type": "bigdecimal",
      "required": true
    }
  }
}
```

## Documentation

- [Script Task](../bpmn/elements/script-task.md) — JavaScript (Nashorn) script format
- [Process Extensions](../bpmn/reference/process-extensions.md) — `bigdecimal` variable type and supported data types

## Related Links

- [GitHub release](https://github.com/Activiti/Activiti/releases/tag/8.3.0)
- [Full changelog](https://github.com/Activiti/Activiti/compare/8.2.0...8.3.0)
- [Previous release: 8.2.0](./v8-2-0.md)
- [Next release: 8.3.1](./v8-3-1.md)
